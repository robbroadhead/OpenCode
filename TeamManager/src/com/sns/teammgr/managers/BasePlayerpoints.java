package com.sns.teammgr.managers;



import java.sql.Connection;
import java.util.ArrayList;
import java.util.Date;
import java.util.Collections;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;

   
      
   
/**
 * This class was autogenerated by Torque on:
 *
 * [Tue Feb 19 16:03:50 CST 2008]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to Playerpoints
 */
public abstract class BasePlayerpoints extends BaseObject
{
    /** The Peer class */
    private static final PlayerpointsPeer peer =
        new PlayerpointsPeer();

                  
        /**
         * The value for the playerid field
         */
        private int playerid;
              
        /**
         * The value for the gameid field
         */
        private int gameid;
              
        /**
         * The value for the pointtype field
         */
        private int pointtype;
      
      
        /**
         * Get the Playerid
         *
         * @return int
         */
        public int getPlayerid()
        {
            return playerid;
        }

                                                      
        /**
         * Set the value of Playerid
         *
         * @param v new value
         */
        public void setPlayerid(int v) throws TorqueException
        {
          


         if (this.playerid != v)
        {
             this.playerid = v;
            setModified(true);
        }

                                          
                if (aPlayer != null && !(aPlayer.getPlayerid()==v))
                {
            aPlayer = null;
        }
          
                       }


        /**
         * Get the Gameid
         *
         * @return int
         */
        public int getGameid()
        {
            return gameid;
        }

                                                      
        /**
         * Set the value of Gameid
         *
         * @param v new value
         */
        public void setGameid(int v) throws TorqueException
        {
          


         if (this.gameid != v)
        {
             this.gameid = v;
            setModified(true);
        }

                                          
                if (aGame != null && !(aGame.getGameid()==v))
                {
            aGame = null;
        }
          
                       }


        /**
         * Get the Pointtype
         *
         * @return int
         */
        public int getPointtype()
        {
            return pointtype;
        }

                                            
        /**
         * Set the value of Pointtype
         *
         * @param v new value
         */
        public void setPointtype(int v) 
        {
          


         if (this.pointtype != v)
        {
             this.pointtype = v;
            setModified(true);
        }

                  
                       }


 
     
   
             
   
       private Player aPlayer;

    /**
     * Declares an association between this object and a Player object
     *
     * @param v Player
     * @throws TorqueException
     */
    public void setPlayer(Player v) throws TorqueException
    {
           if (v == null)
        {
                        setPlayerid(0);
                    }
        else
        {
            setPlayerid(v.getPlayerid());
        }
           aPlayer = v;
    }

                 
    /**
     * Get the associated Player object
     *
     * @return the associated Player object
     * @throws TorqueException
     */
    public Player getPlayer() throws TorqueException
    {
        if (aPlayer == null && (this.playerid > 0))
        {
              aPlayer = PlayerPeer.retrieveByPK(SimpleKey.keyFor(this.playerid));
      
            /* The following can be used instead of the line above to
               guarantee the related object contains a reference
               to this object, but this level of coupling
               may be undesirable in many circumstances.
               As it can lead to a db query with many results that may
               never be used.
               Player obj = PlayerPeer.retrieveByPK(this.playerid);
               obj.addPlayerpointss(this);
             */
        }
        return aPlayer;
    }

    /**
     * Provides convenient way to set a relationship based on a
     * ObjectKey.  e.g.
     * <code>bar.setFooKey(foo.getPrimaryKey())</code>
     *
     */
    public void setPlayerKey(ObjectKey key) throws TorqueException
    {
    
                                        setPlayerid(((NumberKey) key).intValue());
                    }
 
   
             
   
       private Game aGame;

    /**
     * Declares an association between this object and a Game object
     *
     * @param v Game
     * @throws TorqueException
     */
    public void setGame(Game v) throws TorqueException
    {
           if (v == null)
        {
                        setGameid(0);
                    }
        else
        {
            setGameid(v.getGameid());
        }
           aGame = v;
    }

                 
    /**
     * Get the associated Game object
     *
     * @return the associated Game object
     * @throws TorqueException
     */
    public Game getGame() throws TorqueException
    {
        if (aGame == null && (this.gameid > 0))
        {
              aGame = GamePeer.retrieveByPK(SimpleKey.keyFor(this.gameid));
      
            /* The following can be used instead of the line above to
               guarantee the related object contains a reference
               to this object, but this level of coupling
               may be undesirable in many circumstances.
               As it can lead to a db query with many results that may
               never be used.
               Game obj = GamePeer.retrieveByPK(this.gameid);
               obj.addPlayerpointss(this);
             */
        }
        return aGame;
    }

    /**
     * Provides convenient way to set a relationship based on a
     * ObjectKey.  e.g.
     * <code>bar.setFooKey(foo.getPrimaryKey())</code>
     *
     */
    public void setGameKey(ObjectKey key) throws TorqueException
    {
    
                                        setGameid(((NumberKey) key).intValue());
                    }
    
        
    
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
      if (fieldNames == null)
      {
        fieldNames = new ArrayList();
            fieldNames.add("Playerid");
            fieldNames.add("Gameid");
            fieldNames.add("Pointtype");
            fieldNames = Collections.unmodifiableList(fieldNames);
      }
      return fieldNames;
    }

    /**
     * Retrieves a field from the object by name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
            if (name.equals("Playerid"))
    {
              return new Integer(getPlayerid());
          }
            if (name.equals("Gameid"))
    {
              return new Integer(getGameid());
          }
            if (name.equals("Pointtype"))
    {
              return new Integer(getPointtype());
          }
            return null;
    }
    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
            if (name.equals(PlayerpointsPeer.PLAYERID))
        {
              return new Integer(getPlayerid());
          }
            if (name.equals(PlayerpointsPeer.GAMEID))
        {
              return new Integer(getGameid());
          }
            if (name.equals(PlayerpointsPeer.POINTTYPE))
        {
              return new Integer(getPointtype());
          }
            return null;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
            if (pos == 0)
    {
              return new Integer(getPlayerid());
          }
            if (pos == 1)
    {
              return new Integer(getGameid());
          }
            if (pos == 2)
    {
              return new Integer(getPointtype());
          }
                return null;
    }

     


    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
             save(PlayerpointsPeer.getMapBuilder()
                .getDatabaseMap().getName());
     }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
     *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
         try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }

     }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
        if (!alreadyInSave)
      {
        alreadyInSave = true;



  
        // If this object has been modified, then save it to the database.
        if (isModified())
        {
            if (isNew())
            {
                PlayerpointsPeer.doInsert((Playerpoints) this, con);
                setNew(false);
            }
            else
            {
                PlayerpointsPeer.doUpdate((Playerpoints) this, con);
            }
        }

              alreadyInSave = false;
      }
      }




    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
        return null;
    }

 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     */
    public Playerpoints copy() throws TorqueException
    {
        return copyInto(new Playerpoints());
    }

    protected Playerpoints copyInto(Playerpoints copyObj) throws TorqueException
    {
        copyObj.setPlayerid(playerid);
        copyObj.setGameid(gameid);
        copyObj.setPointtype(pointtype);

            

  
        return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public PlayerpointsPeer getPeer()
    {
        return peer;
    }
}
