package com.sns.teammgr.managers;

import java.util.Iterator;
import java.util.List;

import org.apache.torque.util.Criteria;

/**
 * The skeleton for this class was autogenerated by Torque on:
 * 
 * [Fri Aug 03 14:22:15 CDT 2007]
 * 
 * You should add additional methods to this class to meet the application
 * requirements. This class will only be generated as long as it does not
 * already exist in the output directory.
 */
public class EventPeer extends com.sns.teammgr.managers.BaseEventPeer {

	static public void delete(String theId) {
		int currentId = Integer.parseInt(theId);

		Criteria crit = new Criteria();

		try {
			crit.add(EventPeer.EVENTID, currentId);
			crit.setAll();
			EventPeer.doDelete(crit);
		} catch (Exception e) {
			e.printStackTrace();
		}
	}

	/**
	 * This returns a list of Event objects where each 
	 * Event object maps to a row in the database. This is primarily
	 * for drop down lists in the GUI or Event listings. 
	 * 
	 * @return Lkptype[] 
	 */
	static public Event [] getEvents(int teamId) {
		Event theList[] = null;
		int count=0;
		
		Criteria crit = new Criteria();

		try {
			crit.add(EventPeer.TEAMID,teamId);
			List result = EventPeer.doSelect(crit);
			Iterator it = result.iterator();
			theList = new Event[result.size()];
			
			while (it.hasNext()) {
				theList[count++] = (Event) it.next();
			}
		} catch (Exception e) {
			e.printStackTrace();
		}
		
		return theList;
	}
}
