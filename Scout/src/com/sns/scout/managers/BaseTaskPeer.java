package com.sns.scout.managers;

import java.math.BigDecimal;
import java.sql.Connection;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.Date;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;

import org.apache.torque.Torque;
import org.apache.torque.TorqueException;
import org.apache.torque.map.MapBuilder;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.util.BasePeer;
import org.apache.torque.util.Criteria;

import com.workingdogs.village.DataSetException;
import com.workingdogs.village.QueryDataSet;
import com.workingdogs.village.Record;

// Local classes
import com.sns.scout.managers.map.*;


   
   
   
   
   
   
/**
 * This class was autogenerated by Torque on:
 *
 * [Tue Jan 19 16:39:36 CST 2010]
 *
 */
public abstract class BaseTaskPeer
    extends BasePeer
{

    /** the default database name for this class */
    public static final String DATABASE_NAME = "scout";

     /** the table name for this class */
    public static final String TABLE_NAME = "Task";

    /**
     * @return the map builder for this peer
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static MapBuilder getMapBuilder()
        throws TorqueException
    {
        return getMapBuilder(TaskMapBuilder.CLASS_NAME);
    }

    /** the column name for the TASKID field */
    public static final String TASKID;
    /** the column name for the PROJECTID field */
    public static final String PROJECTID;
    /** the column name for the CREATED field */
    public static final String CREATED;
    /** the column name for the STATUSID field */
    public static final String STATUSID;
    /** the column name for the TYPEID field */
    public static final String TYPEID;
    /** the column name for the SUMMARY field */
    public static final String SUMMARY;
    /** the column name for the DETAIL field */
    public static final String DETAIL;
    /** the column name for the OWNERID field */
    public static final String OWNERID;
    /** the column name for the PRIORITYID field */
    public static final String PRIORITYID;
    /** the column name for the STATUSDT field */
    public static final String STATUSDT;
    /** the column name for the ASSIGNED field */
    public static final String ASSIGNED;

    static
    {
    TASKID = "Task.TASKID";
    PROJECTID = "Task.PROJECTID";
    CREATED = "Task.CREATED";
    STATUSID = "Task.STATUSID";
    TYPEID = "Task.TYPEID";
    SUMMARY = "Task.SUMMARY";
    DETAIL = "Task.DETAIL";
    OWNERID = "Task.OWNERID";
    PRIORITYID = "Task.PRIORITYID";
    STATUSDT = "Task.STATUSDT";
    ASSIGNED = "Task.ASSIGNED";

        if (Torque.isInit())
        {
            try
            {
                getMapBuilder();
            }
            catch (Exception e)
            {
                category.error("Could not initialize Peer", e);
            }
        }
        else
        {
            Torque.registerMapBuilder(TaskMapBuilder.CLASS_NAME);
        }
    }

 
    /** number of columns for this peer */
    public static final int numColumns =  11;

    /** A class that can be returned by this peer. */
    protected static final String CLASSNAME_DEFAULT =
        "com.sns.scout.managers.Task";

    /** A class that can be returned by this peer. */
    protected static final Class CLASS_DEFAULT = initClass(CLASSNAME_DEFAULT);

    /**
     * Class object initialization method.
     *
     * @param className name of the class to initialize
     * @return the initialized class
     */
    private static Class initClass(String className)
    {
        Class c = null;
        try
        {
            c = Class.forName(className);
        }
        catch (Throwable t)
        {
            category.error("A FATAL ERROR has occurred which should not "
                + "have happened under any circumstance.  Please notify "
                + "the Torque developers <torque-dev@db.apache.org> "
                + "and give as many details as possible (including the error "
                + "stack trace).", t);

            // Error objects should always be propogated.
            if (t instanceof Error)
            {
                throw (Error) t.fillInStackTrace();
            }
        }
        return c;
    }


    /**
     * Get the list of objects for a ResultSet.  Please not that your
     * resultset MUST return columns in the right order.  You can use
     * getFieldNames() in BaseObject to get the correct sequence.
     *
     * @param results the ResultSet
     * @return the list of objects
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List resultSet2Objects(java.sql.ResultSet results)
            throws TorqueException
    {
        try
        {
            QueryDataSet qds = null;
            List rows = null;
            try
            {
                qds = new QueryDataSet(results);
                rows = getSelectResults(qds);
            }
            finally
            {
                if (qds != null)
                {
                    qds.close();
                }
            }

            return populateObjects(rows);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
        catch (DataSetException e)
        {
            throw new TorqueException(e);
        }
    }



    /**
     * Method to do inserts.
     *
     * @param criteria object used to create the INSERT statement.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static ObjectKey doInsert(Criteria criteria)
        throws TorqueException
    {
        return BaseTaskPeer
            .doInsert(criteria, (Connection) null);
    }

    /**
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of
     * the connection details internally.
     *
     * @param criteria object used to create the INSERT statement.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static ObjectKey doInsert(Criteria criteria, Connection con)
        throws TorqueException
    {
                                                                                                                                                               
        // Set the correct dbName if it has not been overridden
        // criteria.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (criteria.getDbName() == Torque.getDefaultDB())
        {
            criteria.setDbName(DATABASE_NAME);
        }
        if (con == null)
        {
            return BasePeer.doInsert(criteria);
        }
        else
        {
            return BasePeer.doInsert(criteria, con);
        }
    }

    /**
     * Add all the columns needed to create a new object.
     *
     * @param criteria object containing the columns to add.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void addSelectColumns(Criteria criteria)
            throws TorqueException
    {
            criteria.addSelectColumn(TASKID);
            criteria.addSelectColumn(PROJECTID);
            criteria.addSelectColumn(CREATED);
            criteria.addSelectColumn(STATUSID);
            criteria.addSelectColumn(TYPEID);
            criteria.addSelectColumn(SUMMARY);
            criteria.addSelectColumn(DETAIL);
            criteria.addSelectColumn(OWNERID);
            criteria.addSelectColumn(PRIORITYID);
            criteria.addSelectColumn(STATUSDT);
            criteria.addSelectColumn(ASSIGNED);
        }

    /**
     * Create a new object of type cls from a resultset row starting
     * from a specified offset.  This is done so that you can select
     * other rows than just those needed for this object.  You may
     * for example want to create two objects from the same row.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static Task row2Object(Record row,
                                             int offset,
                                             Class cls)
        throws TorqueException
    {
        try
        {
            Task obj = (Task) cls.newInstance();
            populateObject(row, offset, obj);
                            obj.setModified(false);
                        obj.setNew(false);

            return obj;
        }
        catch (InstantiationException e)
        {
            throw new TorqueException(e);
        }
        catch (IllegalAccessException e)
        {
            throw new TorqueException(e);
        }
    }

    /**
     * Populates an object from a resultset row starting
     * from a specified offset.  This is done so that you can select
     * other rows than just those needed for this object.  You may
     * for example want to create two objects from the same row.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void populateObject(Record row,
                                      int offset,
                                      Task obj)
        throws TorqueException
    {
        try
        {
                            obj.setTaskid(row.getValue(offset + 0).asInt());
                                obj.setProjectid(row.getValue(offset + 1).asInt());
                                obj.setCreated(row.getValue(offset + 2).asUtilDate());
                                obj.setStatusid(row.getValue(offset + 3).asInt());
                                obj.setTypeid(row.getValue(offset + 4).asInt());
                                obj.setSummary(row.getValue(offset + 5).asString());
                                obj.setDetail(row.getValue(offset + 6).asString());
                                obj.setOwnerid(row.getValue(offset + 7).asInt());
                                obj.setPriorityid(row.getValue(offset + 8).asInt());
                                obj.setStatusdt(row.getValue(offset + 9).asUtilDate());
                                obj.setAssigned(row.getValue(offset + 10).asInt());
                            }
        catch (DataSetException e)
        {
            throw new TorqueException(e);
        }
    }

    /**
     * Method to do selects.
     *
     * @param criteria object used to create the SELECT statement.
     * @return List of selected Objects
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelect(Criteria criteria) throws TorqueException
    {
        return populateObjects(doSelectVillageRecords(criteria));
    }

    /**
     * Method to do selects within a transaction.
     *
     * @param criteria object used to create the SELECT statement.
     * @param con the connection to use
     * @return List of selected Objects
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelect(Criteria criteria, Connection con)
        throws TorqueException
    {
        return populateObjects(doSelectVillageRecords(criteria, con));
    }

    /**
     * Grabs the raw Village records to be formed into objects.
     * This method handles connections internally.  The Record objects
     * returned by this method should be considered readonly.  Do not
     * alter the data and call save(), your results may vary, but are
     * certainly likely to result in hard to track MT bugs.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelectVillageRecords(Criteria criteria)
        throws TorqueException
    {
        return BaseTaskPeer
            .doSelectVillageRecords(criteria, (Connection) null);
    }

    /**
     * Grabs the raw Village records to be formed into objects.
     * This method should be used for transactions
     *
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelectVillageRecords(Criteria criteria, Connection con)
        throws TorqueException
    {
    
        if (criteria.getSelectColumns().size() == 0)
        {
            addSelectColumns(criteria);
        }

                                                                                                                                                               
        // Set the correct dbName if it has not been overridden
        // criteria.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (criteria.getDbName() == Torque.getDefaultDB())
        {
            criteria.setDbName(DATABASE_NAME);
        }
        // BasePeer returns a List of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        if (con == null)
        {
            return BasePeer.doSelect(criteria);
        }
        else
        {
            return BasePeer.doSelect(criteria, con);
        }
    }

    /**
     * The returned List will contain objects of the default type or
     * objects that inherit from the default.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List populateObjects(List records)
        throws TorqueException
    {
        List results = new ArrayList(records.size());

        // populate the object(s)
        for (int i = 0; i < records.size(); i++)
        {
            Record row = (Record) records.get(i);
            results.add(TaskPeer.row2Object(row, 1,
                TaskPeer.getOMClass()));
        }
        return results;
    }
 

    /**
     * The class that the Peer will make instances of.
     * If the BO is abstract then you must implement this method
     * in the BO.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static Class getOMClass()
        throws TorqueException
    {
            return CLASS_DEFAULT;
        }


    /**
     * Method to do updates.
     *
     * @param criteria object containing data that is used to create the UPDATE
     *        statement.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(Criteria criteria) throws TorqueException
    {
         BaseTaskPeer
            .doUpdate(criteria, (Connection) null);
    }

    /**
     * Method to do updates.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Criteria) method.  It will take care of
     * the connection details internally.
     *
     * @param criteria object containing data that is used to create the UPDATE
     *        statement.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(Criteria criteria, Connection con)
        throws TorqueException
    {
        Criteria selectCriteria = new Criteria(DATABASE_NAME, 2);
                                selectCriteria.put(TASKID, criteria.remove(TASKID));
                                                                                                                                                                                                                                                    
        // Set the correct dbName if it has not been overridden
        // criteria.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (criteria.getDbName() == Torque.getDefaultDB())
        {
            criteria.setDbName(DATABASE_NAME);
        }
        if (con == null)
        {
            BasePeer.doUpdate(selectCriteria, criteria);
        }
        else
        {
            BasePeer.doUpdate(selectCriteria, criteria, con);
        }
    }

    /**
     * Method to do deletes.
     *
     * @param criteria object containing data that is used DELETE from database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
     public static void doDelete(Criteria criteria) throws TorqueException
     {
         BaseTaskPeer
            .doDelete(criteria, (Connection) null);
     }

    /**
     * Method to do deletes.  This method is to be used during a transaction,
     * otherwise use the doDelete(Criteria) method.  It will take care of
     * the connection details internally.
     *
     * @param criteria object containing data that is used DELETE from database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
     public static void doDelete(Criteria criteria, Connection con)
        throws TorqueException
     {
                                                                                                                                                               
        // Set the correct dbName if it has not been overridden
        // criteria.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (criteria.getDbName() == Torque.getDefaultDB())
        {
            criteria.setDbName(DATABASE_NAME);
        }
        if (con == null)
        {
            BasePeer.doDelete(criteria);
        }
        else
        {
            BasePeer.doDelete(criteria, con);
        }
     }

    /**
     * Method to do selects
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelect(Task obj) throws TorqueException
    {
        return doSelect(buildCriteria(obj));
    }

    /**
     * Method to do inserts
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doInsert(Task obj) throws TorqueException
    {
                obj.setPrimaryKey(doInsert(buildCriteria(obj)));
                obj.setNew(false);
        obj.setModified(false);
    }

    /**
     * @param obj the data object to update in the database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(Task obj) throws TorqueException
    {
        doUpdate(buildCriteria(obj));
        obj.setModified(false);
    }

    /**
     * @param obj the data object to delete in the database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(Task obj) throws TorqueException
    {
        doDelete(buildCriteria(obj));
    }

    /**
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Task) method.  It will take
     * care of the connection details internally.
     *
     * @param obj the data object to insert into the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doInsert(Task obj, Connection con)
        throws TorqueException
    {
                obj.setPrimaryKey(doInsert(buildCriteria(obj), con));
                obj.setNew(false);
        obj.setModified(false);
    }

    /**
     * Method to do update.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Task) method.  It will take
     * care of the connection details internally.
     *
     * @param obj the data object to update in the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(Task obj, Connection con)
        throws TorqueException
    {
        doUpdate(buildCriteria(obj), con);
        obj.setModified(false);
    }

    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(Task) method.  It will take
     * care of the connection details internally.
     *
     * @param obj the data object to delete in the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(Task obj, Connection con)
        throws TorqueException
    {
        doDelete(buildCriteria(obj), con);
    }

    /**
     * Method to do deletes.
     *
     * @param pk ObjectKey that is used DELETE from database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(ObjectKey pk) throws TorqueException
    {
        BaseTaskPeer
           .doDelete(pk, (Connection) null);
    }

    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(ObjectKey) method.  It will take
     * care of the connection details internally.
     *
     * @param pk the primary key for the object to delete in the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(ObjectKey pk, Connection con)
        throws TorqueException
    {
        doDelete(buildCriteria(pk), con);
    }

    /** Build a Criteria object from an ObjectKey */
    public static Criteria buildCriteria( ObjectKey pk )
    {
        Criteria criteria = new Criteria();
              criteria.add(TASKID, pk);
          return criteria;
     }

    /** Build a Criteria object from the data object for this peer */
    public static Criteria buildCriteria( Task obj )
    {
        Criteria criteria = new Criteria(DATABASE_NAME);
                            if (!obj.isNew())
                       criteria.add(TASKID, obj.getTaskid());
                                criteria.add(PROJECTID, obj.getProjectid());
                                criteria.add(CREATED, obj.getCreated());
                                criteria.add(STATUSID, obj.getStatusid());
                                criteria.add(TYPEID, obj.getTypeid());
                                criteria.add(SUMMARY, obj.getSummary());
                                criteria.add(DETAIL, obj.getDetail());
                                criteria.add(OWNERID, obj.getOwnerid());
                                criteria.add(PRIORITYID, obj.getPriorityid());
                                criteria.add(STATUSDT, obj.getStatusdt());
                                criteria.add(ASSIGNED, obj.getAssigned());
                return criteria;
    }

 

    
    /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static Task retrieveByPK(int pk)
        throws TorqueException
    {
        return retrieveByPK(SimpleKey.keyFor(pk));
    }

    /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static Task retrieveByPK(ObjectKey pk)
        throws TorqueException
    {
        Connection db = null;
        Task retVal = null;
        try
        {
            db = Torque.getConnection(DATABASE_NAME);
            retVal = retrieveByPK(pk, db);
        }
        finally
        {
            Torque.closeConnection(db);
        }
        return(retVal);
    }

    /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static Task retrieveByPK(ObjectKey pk, Connection con)
        throws TorqueException
    {
        Criteria criteria = buildCriteria(pk);
        List v = doSelect(criteria, con);
        if (v.size() != 1)
        {
            throw new TorqueException("Failed to select one and only one row.");
        }
        else
        {
            return (Task)v.get(0);
        }
    }

    /**
     * Retrieve a multiple objects by pk
     *
     * @param pks List of primary keys
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List retrieveByPKs(List pks)
        throws TorqueException
    {
        Connection db = null;
        List retVal = null;
        try
        {
           db = Torque.getConnection(DATABASE_NAME);
           retVal = retrieveByPKs(pks, db);
        }
        finally
        {
            Torque.closeConnection(db);
        }
        return(retVal);
    }

    /**
     * Retrieve a multiple objects by pk
     *
     * @param pks List of primary keys
     * @param dbcon the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List retrieveByPKs( List pks, Connection dbcon )
        throws TorqueException
    {
        List objs = null;
        if (pks == null || pks.size() == 0)
        {
            objs = new LinkedList();
        }
        else
        {
            Criteria criteria = new Criteria();
              criteria.addIn( TASKID, pks );
          objs = doSelect(criteria, dbcon);
        }
        return objs;
    }

 



          
          
                       
          
          


    /**
     * selects a collection of Task objects pre-filled with their
     * Project objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinProject(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        TaskPeer.addSelectColumns(c);
        int offset = numColumns + 1;
        ProjectPeer.addSelectColumns(c);


                  c.addJoin(TaskPeer.PROJECTID,
            ProjectPeer.PROJECTID);
     

                                                                                                                                                               
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record) rows.get(i);

            Class omClass = TaskPeer.getOMClass();

            Task obj1 = (Task) TaskPeer
                .row2Object(row, 1, omClass);


             omClass = ProjectPeer.getOMClass();
            Project obj2 = (Project)ProjectPeer
                .row2Object(row, offset, omClass);

            boolean newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Project temp_obj2 = (Project)temp_obj1.getProject();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
            results.add(obj1);
        }
        return results;
    }
            
                       
          
          


    /**
     * selects a collection of Task objects pre-filled with their
     * Lkptype objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinLkptype(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        TaskPeer.addSelectColumns(c);
        int offset = numColumns + 1;
        LkptypePeer.addSelectColumns(c);


                  c.addJoin(TaskPeer.TYPEID,
            LkptypePeer.TYPEID);
     

                                                                                                                                                               
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record) rows.get(i);

            Class omClass = TaskPeer.getOMClass();

            Task obj1 = (Task) TaskPeer
                .row2Object(row, 1, omClass);


             omClass = LkptypePeer.getOMClass();
            Lkptype obj2 = (Lkptype)LkptypePeer
                .row2Object(row, offset, omClass);

            boolean newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkptype temp_obj2 = (Lkptype)temp_obj1.getLkptype();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
            results.add(obj1);
        }
        return results;
    }
            
                       
          
          


    /**
     * selects a collection of Task objects pre-filled with their
     * Lkpstatus objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinLkpstatus(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        TaskPeer.addSelectColumns(c);
        int offset = numColumns + 1;
        LkpstatusPeer.addSelectColumns(c);


                  c.addJoin(TaskPeer.STATUSID,
            LkpstatusPeer.STATUSID);
     

                                                                                                                                                               
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record) rows.get(i);

            Class omClass = TaskPeer.getOMClass();

            Task obj1 = (Task) TaskPeer
                .row2Object(row, 1, omClass);


             omClass = LkpstatusPeer.getOMClass();
            Lkpstatus obj2 = (Lkpstatus)LkpstatusPeer
                .row2Object(row, offset, omClass);

            boolean newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkpstatus temp_obj2 = (Lkpstatus)temp_obj1.getLkpstatus();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
            results.add(obj1);
        }
        return results;
    }
            
                       
          
          


    /**
     * selects a collection of Task objects pre-filled with their
     * Lkppriority objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinLkppriority(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        TaskPeer.addSelectColumns(c);
        int offset = numColumns + 1;
        LkppriorityPeer.addSelectColumns(c);


                  c.addJoin(TaskPeer.PRIORITYID,
            LkppriorityPeer.PRIORITYID);
     

                                                                                                                                                               
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record) rows.get(i);

            Class omClass = TaskPeer.getOMClass();

            Task obj1 = (Task) TaskPeer
                .row2Object(row, 1, omClass);


             omClass = LkppriorityPeer.getOMClass();
            Lkppriority obj2 = (Lkppriority)LkppriorityPeer
                .row2Object(row, offset, omClass);

            boolean newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkppriority temp_obj2 = (Lkppriority)temp_obj1.getLkppriority();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
            results.add(obj1);
        }
        return results;
    }
            
                               
          
          


    /**
     * selects a collection of Task objects pre-filled with their
     * Users objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinUsersRelatedByOwnerid(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        TaskPeer.addSelectColumns(c);
        int offset = numColumns + 1;
        UsersPeer.addSelectColumns(c);


                  c.addJoin(TaskPeer.OWNERID,
            UsersPeer.USERID);
     

                                                                                                                                                               
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record) rows.get(i);

            Class omClass = TaskPeer.getOMClass();

            Task obj1 = (Task) TaskPeer
                .row2Object(row, 1, omClass);


             omClass = UsersPeer.getOMClass();
            Users obj2 = (Users)UsersPeer
                .row2Object(row, offset, omClass);

            boolean newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj2 = (Users)temp_obj1.getUsersRelatedByOwnerid();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTaskRelatedByOwnerid(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasksRelatedByOwnerid();
                obj2.addTaskRelatedByOwnerid(obj1);
            }
            results.add(obj1);
        }
        return results;
    }
            
                               
          
          


    /**
     * selects a collection of Task objects pre-filled with their
     * Users objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinUsersRelatedByAssigned(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        TaskPeer.addSelectColumns(c);
        int offset = numColumns + 1;
        UsersPeer.addSelectColumns(c);


                  c.addJoin(TaskPeer.ASSIGNED,
            UsersPeer.USERID);
     

                                                                                                                                                               
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record) rows.get(i);

            Class omClass = TaskPeer.getOMClass();

            Task obj1 = (Task) TaskPeer
                .row2Object(row, 1, omClass);


             omClass = UsersPeer.getOMClass();
            Users obj2 = (Users)UsersPeer
                .row2Object(row, offset, omClass);

            boolean newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj2 = (Users)temp_obj1.getUsersRelatedByAssigned();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTaskRelatedByAssigned(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasksRelatedByAssigned();
                obj2.addTaskRelatedByAssigned(obj1);
            }
            results.add(obj1);
        }
        return results;
    }
     

  
                                  
    
     
                       
          

    /**
     * selects a collection of Task objects pre-filled with
     * all related objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinAllExceptProject(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to another value
        // so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                              
                LkptypePeer.addSelectColumns(c);
        int offset3 = offset2 + LkptypePeer.numColumns;
                                          
                LkpstatusPeer.addSelectColumns(c);
        int offset4 = offset3 + LkpstatusPeer.numColumns;
                                          
                LkppriorityPeer.addSelectColumns(c);
        int offset5 = offset4 + LkppriorityPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset6 = offset5 + UsersPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset7 = offset6 + UsersPeer.numColumns;
                                                                                                                                                                                             
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record)rows.get(i);

                        Class omClass = TaskPeer.getOMClass();
      
            Task obj1 = (Task)TaskPeer
                .row2Object(row, 1, omClass);


                            
                                  
                                                
                        
            
                 omClass = LkptypePeer.getOMClass();
                Lkptype obj2 = (Lkptype)LkptypePeer
                .row2Object( row, offset2, omClass);

             boolean  newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkptype temp_obj2 = (Lkptype)temp_obj1.getLkptype();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkpstatusPeer.getOMClass();
                Lkpstatus obj3 = (Lkpstatus)LkpstatusPeer
                .row2Object( row, offset3, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkpstatus temp_obj3 = (Lkpstatus)temp_obj1.getLkpstatus();
                if (temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj3.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initTasks();
                obj3.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkppriorityPeer.getOMClass();
                Lkppriority obj4 = (Lkppriority)LkppriorityPeer
                .row2Object( row, offset4, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkppriority temp_obj4 = (Lkppriority)temp_obj1.getLkppriority();
                if (temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj4.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initTasks();
                obj4.addTask(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj5 = (Users)UsersPeer
                .row2Object( row, offset5, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj5 = (Users)temp_obj1.getUsersRelatedByOwnerid();
                if (temp_obj5.getPrimaryKey().equals(obj5.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj5.addTaskRelatedByOwnerid(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj5.initTasksRelatedByOwnerid();
                obj5.addTaskRelatedByOwnerid(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj6 = (Users)UsersPeer
                .row2Object( row, offset6, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj6 = (Users)temp_obj1.getUsersRelatedByAssigned();
                if (temp_obj6.getPrimaryKey().equals(obj6.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj6.addTaskRelatedByAssigned(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj6.initTasksRelatedByAssigned();
                obj6.addTaskRelatedByAssigned(obj1);
            }
                        results.add(obj1);
        }
        return results;
    }
  
     
                       
          

    /**
     * selects a collection of Task objects pre-filled with
     * all related objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinAllExceptLkptype(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to another value
        // so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ProjectPeer.addSelectColumns(c);
        int offset3 = offset2 + ProjectPeer.numColumns;
                                          
                              
                LkpstatusPeer.addSelectColumns(c);
        int offset4 = offset3 + LkpstatusPeer.numColumns;
                                          
                LkppriorityPeer.addSelectColumns(c);
        int offset5 = offset4 + LkppriorityPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset6 = offset5 + UsersPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset7 = offset6 + UsersPeer.numColumns;
                                                                                                                                                                                             
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record)rows.get(i);

                        Class omClass = TaskPeer.getOMClass();
      
            Task obj1 = (Task)TaskPeer
                .row2Object(row, 1, omClass);


                            
                                                
                        
            
                 omClass = ProjectPeer.getOMClass();
                Project obj2 = (Project)ProjectPeer
                .row2Object( row, offset2, omClass);

             boolean  newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Project temp_obj2 = (Project)temp_obj1.getProject();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
                                
                                  
                                                
                        
            
                 omClass = LkpstatusPeer.getOMClass();
                Lkpstatus obj3 = (Lkpstatus)LkpstatusPeer
                .row2Object( row, offset3, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkpstatus temp_obj3 = (Lkpstatus)temp_obj1.getLkpstatus();
                if (temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj3.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initTasks();
                obj3.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkppriorityPeer.getOMClass();
                Lkppriority obj4 = (Lkppriority)LkppriorityPeer
                .row2Object( row, offset4, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkppriority temp_obj4 = (Lkppriority)temp_obj1.getLkppriority();
                if (temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj4.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initTasks();
                obj4.addTask(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj5 = (Users)UsersPeer
                .row2Object( row, offset5, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj5 = (Users)temp_obj1.getUsersRelatedByOwnerid();
                if (temp_obj5.getPrimaryKey().equals(obj5.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj5.addTaskRelatedByOwnerid(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj5.initTasksRelatedByOwnerid();
                obj5.addTaskRelatedByOwnerid(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj6 = (Users)UsersPeer
                .row2Object( row, offset6, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj6 = (Users)temp_obj1.getUsersRelatedByAssigned();
                if (temp_obj6.getPrimaryKey().equals(obj6.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj6.addTaskRelatedByAssigned(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj6.initTasksRelatedByAssigned();
                obj6.addTaskRelatedByAssigned(obj1);
            }
                        results.add(obj1);
        }
        return results;
    }
  
     
                       
          

    /**
     * selects a collection of Task objects pre-filled with
     * all related objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinAllExceptLkpstatus(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to another value
        // so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ProjectPeer.addSelectColumns(c);
        int offset3 = offset2 + ProjectPeer.numColumns;
                                          
                LkptypePeer.addSelectColumns(c);
        int offset4 = offset3 + LkptypePeer.numColumns;
                                          
                              
                LkppriorityPeer.addSelectColumns(c);
        int offset5 = offset4 + LkppriorityPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset6 = offset5 + UsersPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset7 = offset6 + UsersPeer.numColumns;
                                                                                                                                                                                             
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record)rows.get(i);

                        Class omClass = TaskPeer.getOMClass();
      
            Task obj1 = (Task)TaskPeer
                .row2Object(row, 1, omClass);


                            
                                                
                        
            
                 omClass = ProjectPeer.getOMClass();
                Project obj2 = (Project)ProjectPeer
                .row2Object( row, offset2, omClass);

             boolean  newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Project temp_obj2 = (Project)temp_obj1.getProject();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkptypePeer.getOMClass();
                Lkptype obj3 = (Lkptype)LkptypePeer
                .row2Object( row, offset3, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkptype temp_obj3 = (Lkptype)temp_obj1.getLkptype();
                if (temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj3.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initTasks();
                obj3.addTask(obj1);
            }
                                
                                  
                                                
                        
            
                 omClass = LkppriorityPeer.getOMClass();
                Lkppriority obj4 = (Lkppriority)LkppriorityPeer
                .row2Object( row, offset4, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkppriority temp_obj4 = (Lkppriority)temp_obj1.getLkppriority();
                if (temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj4.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initTasks();
                obj4.addTask(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj5 = (Users)UsersPeer
                .row2Object( row, offset5, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj5 = (Users)temp_obj1.getUsersRelatedByOwnerid();
                if (temp_obj5.getPrimaryKey().equals(obj5.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj5.addTaskRelatedByOwnerid(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj5.initTasksRelatedByOwnerid();
                obj5.addTaskRelatedByOwnerid(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj6 = (Users)UsersPeer
                .row2Object( row, offset6, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj6 = (Users)temp_obj1.getUsersRelatedByAssigned();
                if (temp_obj6.getPrimaryKey().equals(obj6.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj6.addTaskRelatedByAssigned(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj6.initTasksRelatedByAssigned();
                obj6.addTaskRelatedByAssigned(obj1);
            }
                        results.add(obj1);
        }
        return results;
    }
  
     
                       
          

    /**
     * selects a collection of Task objects pre-filled with
     * all related objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinAllExceptLkppriority(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to another value
        // so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ProjectPeer.addSelectColumns(c);
        int offset3 = offset2 + ProjectPeer.numColumns;
                                          
                LkptypePeer.addSelectColumns(c);
        int offset4 = offset3 + LkptypePeer.numColumns;
                                          
                LkpstatusPeer.addSelectColumns(c);
        int offset5 = offset4 + LkpstatusPeer.numColumns;
                                          
                              
                UsersPeer.addSelectColumns(c);
        int offset6 = offset5 + UsersPeer.numColumns;
                                          
                UsersPeer.addSelectColumns(c);
        int offset7 = offset6 + UsersPeer.numColumns;
                                                                                                                                                                                             
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record)rows.get(i);

                        Class omClass = TaskPeer.getOMClass();
      
            Task obj1 = (Task)TaskPeer
                .row2Object(row, 1, omClass);


                            
                                                
                        
            
                 omClass = ProjectPeer.getOMClass();
                Project obj2 = (Project)ProjectPeer
                .row2Object( row, offset2, omClass);

             boolean  newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Project temp_obj2 = (Project)temp_obj1.getProject();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkptypePeer.getOMClass();
                Lkptype obj3 = (Lkptype)LkptypePeer
                .row2Object( row, offset3, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkptype temp_obj3 = (Lkptype)temp_obj1.getLkptype();
                if (temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj3.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initTasks();
                obj3.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkpstatusPeer.getOMClass();
                Lkpstatus obj4 = (Lkpstatus)LkpstatusPeer
                .row2Object( row, offset4, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkpstatus temp_obj4 = (Lkpstatus)temp_obj1.getLkpstatus();
                if (temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj4.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initTasks();
                obj4.addTask(obj1);
            }
                                
                                  
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj5 = (Users)UsersPeer
                .row2Object( row, offset5, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj5 = (Users)temp_obj1.getUsersRelatedByOwnerid();
                if (temp_obj5.getPrimaryKey().equals(obj5.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj5.addTaskRelatedByOwnerid(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj5.initTasksRelatedByOwnerid();
                obj5.addTaskRelatedByOwnerid(obj1);
            }
                                
                                                                
                        
            
                 omClass = UsersPeer.getOMClass();
                Users obj6 = (Users)UsersPeer
                .row2Object( row, offset6, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Users temp_obj6 = (Users)temp_obj1.getUsersRelatedByAssigned();
                if (temp_obj6.getPrimaryKey().equals(obj6.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj6.addTaskRelatedByAssigned(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj6.initTasksRelatedByAssigned();
                obj6.addTaskRelatedByAssigned(obj1);
            }
                        results.add(obj1);
        }
        return results;
    }
  
     
                               
          

    /**
     * selects a collection of Task objects pre-filled with
     * all related objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinAllExceptUsersRelatedByOwnerid(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to another value
        // so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ProjectPeer.addSelectColumns(c);
        int offset3 = offset2 + ProjectPeer.numColumns;
                                          
                LkptypePeer.addSelectColumns(c);
        int offset4 = offset3 + LkptypePeer.numColumns;
                                          
                LkpstatusPeer.addSelectColumns(c);
        int offset5 = offset4 + LkpstatusPeer.numColumns;
                                          
                LkppriorityPeer.addSelectColumns(c);
        int offset6 = offset5 + LkppriorityPeer.numColumns;
                                          
                              
                                                                                                                                                                                 
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record)rows.get(i);

                        Class omClass = TaskPeer.getOMClass();
      
            Task obj1 = (Task)TaskPeer
                .row2Object(row, 1, omClass);


                            
                                                
                        
            
                 omClass = ProjectPeer.getOMClass();
                Project obj2 = (Project)ProjectPeer
                .row2Object( row, offset2, omClass);

             boolean  newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Project temp_obj2 = (Project)temp_obj1.getProject();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkptypePeer.getOMClass();
                Lkptype obj3 = (Lkptype)LkptypePeer
                .row2Object( row, offset3, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkptype temp_obj3 = (Lkptype)temp_obj1.getLkptype();
                if (temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj3.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initTasks();
                obj3.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkpstatusPeer.getOMClass();
                Lkpstatus obj4 = (Lkpstatus)LkpstatusPeer
                .row2Object( row, offset4, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkpstatus temp_obj4 = (Lkpstatus)temp_obj1.getLkpstatus();
                if (temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj4.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initTasks();
                obj4.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkppriorityPeer.getOMClass();
                Lkppriority obj5 = (Lkppriority)LkppriorityPeer
                .row2Object( row, offset5, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkppriority temp_obj5 = (Lkppriority)temp_obj1.getLkppriority();
                if (temp_obj5.getPrimaryKey().equals(obj5.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj5.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj5.initTasks();
                obj5.addTask(obj1);
            }
                                
                                  
                          results.add(obj1);
        }
        return results;
    }
  
     
                               
          

    /**
     * selects a collection of Task objects pre-filled with
     * all related objects.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TaskPeer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static List doSelectJoinAllExceptUsersRelatedByAssigned(Criteria c)
        throws TorqueException
    {
    
        // Set the correct dbName if it has not been overridden
        // c.getDbName will return the same object if not set to another value
        // so == check is okay and faster
        if (c.getDbName() == Torque.getDefaultDB())
        {
            c.setDbName(DATABASE_NAME);
        }

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ProjectPeer.addSelectColumns(c);
        int offset3 = offset2 + ProjectPeer.numColumns;
                                          
                LkptypePeer.addSelectColumns(c);
        int offset4 = offset3 + LkptypePeer.numColumns;
                                          
                LkpstatusPeer.addSelectColumns(c);
        int offset5 = offset4 + LkpstatusPeer.numColumns;
                                          
                LkppriorityPeer.addSelectColumns(c);
        int offset6 = offset5 + LkppriorityPeer.numColumns;
                                          
                              
                                                                                                                                                                                 
        List rows = BasePeer.doSelect(c);
        List results = new ArrayList();

        for (int i = 0; i < rows.size(); i++)
        {
            Record row = (Record)rows.get(i);

                        Class omClass = TaskPeer.getOMClass();
      
            Task obj1 = (Task)TaskPeer
                .row2Object(row, 1, omClass);


                            
                                                
                        
            
                 omClass = ProjectPeer.getOMClass();
                Project obj2 = (Project)ProjectPeer
                .row2Object( row, offset2, omClass);

             boolean  newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Project temp_obj2 = (Project)temp_obj1.getProject();
                if (temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj2.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initTasks();
                obj2.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkptypePeer.getOMClass();
                Lkptype obj3 = (Lkptype)LkptypePeer
                .row2Object( row, offset3, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkptype temp_obj3 = (Lkptype)temp_obj1.getLkptype();
                if (temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj3.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initTasks();
                obj3.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkpstatusPeer.getOMClass();
                Lkpstatus obj4 = (Lkpstatus)LkpstatusPeer
                .row2Object( row, offset4, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkpstatus temp_obj4 = (Lkpstatus)temp_obj1.getLkpstatus();
                if (temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj4.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initTasks();
                obj4.addTask(obj1);
            }
                                
                                                
                        
            
                 omClass = LkppriorityPeer.getOMClass();
                Lkppriority obj5 = (Lkppriority)LkppriorityPeer
                .row2Object( row, offset5, omClass);

             newObject = true;
            for (int j = 0; j < results.size(); j++)
            {
                Task temp_obj1 = (Task)results.get(j);
                Lkppriority temp_obj5 = (Lkppriority)temp_obj1.getLkppriority();
                if (temp_obj5.getPrimaryKey().equals(obj5.getPrimaryKey()))
                {
                    newObject = false;
                    temp_obj5.addTask(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj5.initTasks();
                obj5.addTask(obj1);
            }
                                
                                  
                          results.add(obj1);
        }
        return results;
    }
    

    /**
     * Returns the TableMap related to this peer.  This method is not
     * needed for general use but a specific application could have a need.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    protected static TableMap getTableMap()
        throws TorqueException
    {
        return Torque.getDatabaseMap(DATABASE_NAME).getTable(TABLE_NAME);
    }
 }
